{
  "name": "NXUS Token Export",
  "id": "nxus-token-export",
  "api": "1.0.0",
  "main": "code.js",
  "ui": "ui.html",
  "editorType": ["figma"],
  "documentAccess": "dynamic-page",
  "networkAccess": {
    "allowedDomains": ["https://api.github.com"],
    "reasoning": "Sends the token export to GitHub as a repository_dispatch, which starts the figma-sync workflow. GitHub is the only host it can reach. The credential is a fine-grained token the user enters once; it lives in figma.clientStorage (per-user, sandboxed) and never in this repository, which is public."
  }
}
